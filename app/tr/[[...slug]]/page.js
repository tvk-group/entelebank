import {notFound} from 'next/navigation';import {tr} from '../../../lib/locales/tr';
const routeMap={about:'about',personal:'personal',business:'business',infrastructure:'infrastructure',security:'security',governance:'governance',regulatory:'regulatory',privacy:'privacy',cookies:'cookies',terms:'terms',roadmap:'roadmap'};
export const metadata={title:'EnteleBANK Türkçe',description:'EnteleBANK Türkçe kurumsal ve düzenleyici bilgi sayfaları.'};
function Sections({items=[]}){return items.map(([h,p])=><section key={h}><h2>{h}</h2><p>{p}</p></section>)}
export default async function TurkishPage({params}){const {slug=[]}=await params;const path=slug[0]||'home';if(slug.length>1)notFound();const key=routeMap[path]||path;const t=tr[key];if(!t)notFound();if(key==='home')return <main className="localeHome"><section className="hero"><p className="eyebrow">{t.eyebrow}</p><h1>{t.title}</h1><p className="lead">{t.lead}</p><a className="button" href="/tr/regulatory">{t.cta}</a><div className="notice"><b>{tr.common.noticeTitle}</b><p>{t.notice}</p></div><p className="fine">{tr.common.authoritative}</p></section></main>;if(key==='roadmap')return <main className="legalPage institutionalPage"><p className="eyebrow">{t.eyebrow}</p><h1>{t.title}</h1><p className="legalLead">{t.lead}</p><div className="roadmap">{t.stages.map(([n,h,p])=><section className="roadmapStep" key={n}><span>{n}</span><div><h2>{h}</h2><p>{p}</p></div></section>)}</div><h2>{t.statusTitle}</h2><p>{t.status}</p><p className="fine">{tr.common.authoritative}</p></main>;return <main className="legalPage institutionalPage"><p className="eyebrow">{t.eyebrow}</p><h1>{t.title}</h1>{t.lead&&<p className="legalLead">{t.lead}</p>}<Sections items={t.sections}/><p className="fine">{tr.common.authoritative}</p></main>}
