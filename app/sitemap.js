import {localeCodes} from '../lib/i18n';
export default function sitemap(){const base='https://www.entelebank.com';const paths=['','/about','/personal','/business','/infrastructure','/security','/governance','/roadmap','/regulatory','/privacy','/cookies','/terms'];const english=paths.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly':'monthly',priority:path===''?1:['/about','/roadmap','/regulatory','/security'].includes(path)?0.8:0.7}));const localized=localeCodes.filter(l=>l!=='en').flatMap(locale=>paths.map(path=>({url:`${base}/${locale}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly':'monthly',priority:path===''?0.9:0.65})));return [...english,...localized]}
