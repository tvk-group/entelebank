'use client';
import {useEffect,useState} from 'react';
export default function PWAInstall(){const [prompt,setPrompt]=useState(null);const [installed,setInstalled]=useState(false);useEffect(()=>{if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;if(standalone){setInstalled(true);return}const before=e=>{e.preventDefault();setPrompt(e)};const done=()=>{setInstalled(true);setPrompt(null)};window.addEventListener('beforeinstallprompt',before);window.addEventListener('appinstalled',done);return()=>{window.removeEventListener('beforeinstallprompt',before);window.removeEventListener('appinstalled',done)}},[]);async function install(){if(!prompt)return;await prompt.prompt();await prompt.userChoice;setPrompt(null)}if(installed||!prompt)return null;return <aside className="pwaInstall" role="status" aria-label="Install EnteleBANK"><img src="/brand/entelebank-mark.svg" alt=""/><div><strong>Install EnteleBANK</strong><span>Add this site as an app on this device.</span></div><button type="button" onClick={install}>Install</button></aside>}
