'use client';
import {useState} from 'react';
export default function MobileNav({base='',labels}){const [open,setOpen]=useState(false);const links=[['about',labels.about],['personal',labels.personal],['business',labels.business],['infrastructure',labels.infrastructure],['security',labels.security],['governance',labels.governance],['roadmap',labels.roadmap],['regulatory',labels.regulatory]];return <div className="mobileNav"><button className="menuButton" type="button" aria-expanded={open} aria-controls="mobile-main-menu" onClick={()=>setOpen(v=>!v)}><span aria-hidden="true">{open?'×':'☰'}</span><b>{labels.menu||'Menu'}</b></button>{open&&<nav id="mobile-main-menu" className="mobileMenu" aria-label={labels.navLabel||'Main navigation'}>{links.map(([path,label])=><a key={path} href={`${base}/${path}`} onClick={()=>setOpen(false)}>{label}</a>)}</nav>}</div>}
