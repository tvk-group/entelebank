import fs from 'node:fs';
const i18n=fs.readFileSync('lib/i18n.js','utf8');
const localeArray=i18n.match(/export const locales\s*=\s*\[([\s\S]*?)\];/)?.[1]||'';
const localeCodes=[...localeArray.matchAll(/\['([a-z]{2})','/g)].map(m=>m[1]);
const failures=[];
if(localeCodes.length!==25) failures.push(`expected 25 configured locales, found ${localeCodes.length}`);
if(new Set(localeCodes).size!==localeCodes.length) failures.push('duplicate locale codes configured');
const registry=fs.readFileSync('lib/fullLocaleRegistry.js','utf8');
if(registry.includes('shellDerivedFull')) failures.push('placeholder-derived content is forbidden');
for(const legacy of ['remainingFull','remainingFullB','remainingFullC','remainingFullD','remainingFullE']) if(registry.includes(`./${legacy}'`)) failures.push(`${legacy} corrupted legacy bundle must not be imported`);
for(const f of ['turkishFull.js','germanFull.js','frenchFull.js','spanishFull.js','italianFull.js','portugueseFull.js','dutchFull.js','recoveredFull.js']) if(!fs.existsSync(`lib/${f}`)) failures.push(`${f}: missing`);
if(fs.existsSync('app/tr/[[...slug]]/page.js')) failures.push('legacy Turkish route duplicates locale architecture');
const generic=fs.readFileSync('app/[locale]/[slug]/page.js','utf8');
if(generic.includes('/visuals/resilience-architecture.svg')) failures.push('localized Infrastructure embeds English SVG');
if(!generic.includes("slug==='infrastructure'")) failures.push('localized Infrastructure model missing');
const shells=fs.readFileSync('lib/localeShells.js','utf8');for(const l of localeCodes.filter(x=>x!=='en')) if(!shells.includes(`${l}:s(`)) failures.push(`${l}: shell missing`);
const home=fs.readFileSync('app/[locale]/page.js','utf8');if(home.includes('<LanguageMenu')) failures.push('duplicate homepage language selector');if(!home.includes('localeDark')) failures.push('localized homepage missing navy institutional section');
if(failures.length){console.error('SITE QUALITY AUDIT FAILED\n- '+failures.join('\n- '));process.exit(1)}console.log(`Site quality structural audit passed: ${localeCodes.length} locales`);
