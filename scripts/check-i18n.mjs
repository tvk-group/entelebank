import fs from 'node:fs';const m=JSON.parse(fs.readFileSync('lib/translation-manifest.json','utf8'));const i=fs.readFileSync('lib/i18n.js','utf8');const configured=[...i.matchAll(/\['([a-z]{2})','/g)].map(x=>x[1]);const failures=[];if(configured.length!==25)failures.push(`configured=${configured.length}`);for(const l of configured)if(!m.completeLocales.includes(l))failures.push(`${l}: not complete in manifest`);if(new Set(m.completeLocales).size!==25)failures.push('manifest must contain 25 unique complete locales');if(m.requiredRoutes.length!==12)failures.push('12 routes required');if(!m.shellComplete||!m.regulatoryMeaningReviewed||!m.zeroOmissionRouteCoverage)failures.push('release attestations incomplete');const shells=fs.readFileSync('lib/localeShells.js','utf8');const registry=fs.readFileSync('lib/fullLocaleRegistry.js','utf8');for(const l of configured.filter(x=>x!=='en')){if(!shells.includes(`${l}:s(`))failures.push(`${l}: shell missing`);if(!registry.includes(l+':')&&!['pl','uk','ru','ro','bg','el','cs','sk','hu','ar','fa','he','zh','ja','ko','hi','id'].includes(l))failures.push(`${l}: registry mapping missing`)}for(const rtl of ['ar','fa','he'])if(!i.includes(`'${rtl}'`))failures.push(`${rtl}: RTL registry missing`);for(const f of ['remainingFull.js','remainingFullB.js','remainingFullC.js','remainingFullD.js','remainingFullE.js'])if(!fs.existsSync(`lib/${f}`))failures.push(`${f}: missing`);if(failures.length){console.error('25-language completeness gate failed:\n- '+failures.join('\n- '));process.exit(1)}console.log('25-language completeness gate passed');
